using UnityEngine;
private class EndlessSpawner.SpawnerStateBase : StateMachine.State
{
    // Properties
    public TzarGames.Endless.EndlessSpawner Spawner { get; }
    
    // Methods
    public EndlessSpawner.SpawnerStateBase()
    {
    
    }
    public TzarGames.Endless.EndlessSpawner get_Spawner()
    {
        if(this.Owner == null)
        {
                return (TzarGames.Endless.EndlessSpawner);
        }
        
        0 = 0;
        return (TzarGames.Endless.EndlessSpawner);
    }

}
